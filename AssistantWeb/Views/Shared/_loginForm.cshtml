@model AssistantWeb.Models.User
<div id="login-form">

    <h2>Login</h2>

    <form method="post" action="login">
        <div class="form-group">
            @Html.LabelFor(u => u.Email)
            @Html.TextBoxFor(u => u.Email, new { @class = "form-control", @type = "email", @placeholder = "example@hotmail.com" })
            <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>
        </div>

        <div class="form-group">
            @Html.LabelFor(u => u.Password)
            @Html.TextBoxFor(u => u.Password, new { @class = "form-control", @type = "password" })
        </div>

        <button class="btn btn-primary" type="submit">Submit</button>
    </form>

    <br />
    <br />

    <label>Don't have an account? register here</label>
    <button class="btn btn-primary" id="go-to-register">Register</button>
</div>
